<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.shifthackz.shopaccounting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
            android:name="com.shifthackz.shopaccounting.App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.add.AddProductActivity"></activity>
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.update.StatsUpdateActivity"></activity>
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.update.ProductUpdateActivity"></activity>
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.detail.ProductDetailActivity"></activity>
        <activity
                android:name="com.shifthackz.shopaccounting.presentation.activities.splash.SplashActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/app_name"
                android:noHistory="true"
                android:screenOrientation="portrait"
                android:theme="@style/NoTittleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.detail.CategoryDetailActivity" />
        <activity android:name="com.shifthackz.shopaccounting.presentation.activities.main.MainActivity" />
    </application>

</manifest>